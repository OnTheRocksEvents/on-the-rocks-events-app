{
  "name": "on-the-rocks-events",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",                        // Servidor de desarrollo rápido y con hot reload
    "build": "vite build --clean",       // Build limpio para producción
    "preview": "vite preview --port 5000", // Vista previa local del build, puerto fijo para evitar conflictos
    "lint": "eslint 'src/**/*.{js,ts,jsx,tsx}' --fix", // Linter automático para código limpio
    "format": "prettier --write 'src/**/*.{js,ts,jsx,tsx,json,css,md}'" // Formateo automático para mantener estilo consistente
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^8.0.0",
    "eslint-plugin-react": "^7.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
